# Building the Control Room Software

To Setup CMake (run this on your first time building the project)

        mkdir build
        cd build
        cmake ../

To build with CMake

        cmake --build build